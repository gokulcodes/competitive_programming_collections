#include <bits/stdc++.h>

using namespace std;

int main(){
    int ts;
    cin >> ts;
    while(ts--){
        int alice, bob;
        cin >> alice >> bob;
        cout << alice-1 << " " << bob << '\n';
    }

    return 0;
}